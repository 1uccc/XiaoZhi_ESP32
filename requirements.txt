# Thư viện Python cần thiết cho script dịch comments
# Cài đặt: pip install -r requirements.txt

# Thư viện dịch (chọn một trong hai)
deep-translator>=1.11.4      # Khuyên dùng, không cần API key
google-cloud-translate>=3.8.0  # Nếu bạn có API key Google Cloud

# Thư viện hỗ trợ (tùy chọn)
requests>=2.28.0             # Cho HTTP requests
